📝 This question explains:

Meaning of traversing

How nodes are visited one by one

Answer:
Traversing in a singly linked list means visiting each node one by one starting from the head node until the last node (NULL).

During traversal:

A temporary pointer starts from head

Data of each node is accessed

Pointer moves using next link

Traversal is always forward only in a singly linked list.