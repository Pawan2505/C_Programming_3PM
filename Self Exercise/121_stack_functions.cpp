// 📝 This question explains:

// Different stack functions

// Purpose of each function

// Answer:

// Push() → Inserts an element into the stack

// Pop() → Removes the top element from the stack

// Peek() → Returns the top element without removing it

// display() → Displays all elements of the stack

// isEmpty() → Checks whether stack is empty

// isFull() → Checks whether stack is full

// size() → Returns total number of elements in stack