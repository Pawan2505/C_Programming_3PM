// Purpose: Use abs() function to find absolute value

#include <stdio.h>
#include <stdlib.h>  // Required for abs()

int main() {

    int num = -56;

    int value = abs(num);

    printf("Absolute Value is : %d\n", value);

    return 0;
}