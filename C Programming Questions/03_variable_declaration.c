// Variable: A container where we store data

#include <stdio.h>

int main() {
    int age;   // Variable declaration
    age = 100; // Initialization
    printf("Your age is : %d\n", age);
    return 0;
}
