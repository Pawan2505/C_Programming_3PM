#include <stdio.h>

int main() {
    int age = 200;  // declaration + initialization
    printf("Your age1 is : %d\n", age);

    age = 500;      // re-initialization (assigning a new value)
    printf("Your age2 (re-initialized) is : %d\n", age);

    return 0;
}
